def main() {
  var buf = [byte(0); 256];
  while (true) {
    var read = __stdin_read__(&buf[0], len(buf));
    if (read <= 0) {
      break;
    }
    __stdout_write__(&buf[0], read);
    if (read < len(buf)) {
      break;
    }
  }
}
